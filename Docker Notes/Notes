
Docker - containerazation

basic commands:
docker version
docker -v
docker info
docker --help docker login

images commands:
docker images
docker pull ubuntu docker rmi <mage id>
container commands
docker ps
docker run ubuntu
docker start ubuntu
docker stop ubuntu
docker rm <container id/name>

system commands:

docker stats
docker system df
docker system prune -f